server:
  port: 10003
  undertow:
    threads:
      # 设置IO线程数，来执行非阻塞任务，负责多个连接数
      io: 16
      # 工作线程数
      worker: 256
    # 每块buffer的空间大小
    buffer-size: 1024
    # 分配堆外内存
    direct-buffers: true

elasticsearch:
  host: 101.33.251.11:9200
  username: ENC(VcPrVKUHLwRVZVtLbkOoGw==)
  password: ENC(AHdloD6Rp4bu9SYZJbO7bcwErNCQm43R1b4zh6x4B10=)

spring:
  boot:
    admin:
      client:
        url: http://192.168.62.137:5000/monitor
        username: ENC(7j+MYBNM2fhq8WST3DE3WA==)
        password: ENC(nj35dMcOWEmR5EASEZR3yfaT4IdT9TPm)
        instance:
          service-base-url: http://192.168.62.137:${server.port}

logging:
  file:
    name: /opt/logs/${spring.application.name}/error.log

app:
  id: elasticsearch # 应用全局唯一的身份标识
apollo:
  cacheDir: config # 自定义本地配置文件缓存路径
  bootstrap:
    enabled: true # 设置在应用启动阶段就加载 Apollo 配置
    eagerLoad:
      enabled: true